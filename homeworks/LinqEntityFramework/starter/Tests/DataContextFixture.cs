using Exercises;

namespace Tests;
/*
public class DataContextFixture : IAsyncDisposable
{
    public ApplicationDataContext Context { get; private set; }

    public DataContextFixture()
    {
        Context = new ApplicationDataContextFactory().CreateDbContext([]);
    }

    public async ValueTask DisposeAsync()
    {
        await Context.DisposeAsync();
        GC.SuppressFinalize(this);
    }
}
*/